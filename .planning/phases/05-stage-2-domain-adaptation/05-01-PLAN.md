---
phase: 05-stage-2-domain-adaptation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [scripts/training_utils.py, scripts/train_stage2.py]
autonomous: true
requirements: [TRN2-01]
must_haves:
  truths:
    - "User can run Stage 2 training from Stage 1 checkpoint"
    - "Training uses lower LR profile (5e-5) than Stage 1"
    - "Checkpoints are saved per-epoch with full resumption capability"
  artifacts:
    - path: scripts/training_utils.py
      provides: Shared training utilities (config loading, checkpointing, metrics logging)
      exports: [load_config, merge_config, validate_config, write_run_manifest, set_seeds]
    - path: scripts/train_stage2.py
      provides: Stage 2 domain adaptation training CLI
      exports: [main]
  key_links:
    - from: scripts/train_stage2.py
      to: scripts/training_utils.py
      via: import
      pattern: "from scripts.training_utils import"
    - from: scripts/train_stage2.py
      to: config/training/stage2.yaml
      via: CLI --config argument
      pattern: "DEFAULT_CONFIG_PATH.*stage2"
---
<objective>
Create Stage 2 training infrastructure with shared utilities and domain adaptation CLI.

Purpose: Establish reusable training patterns extracted from Stage 1, then implement Stage 2 domain adaptation training that loads from Stage 1 checkpoint with lower learning rate.

Output: 
- `scripts/training_utils.py` — Shared utilities for both Stage 1 and Stage 2
- `scripts/train_stage2.py` — Stage 2 training CLI
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-stage-2-domain-adaptation/05-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@scripts/train_stage1.py
@config/training/stage2.yaml
@AGENTS.md
</context>

<tasks>
<task type="auto">
<name>Task 1: Extract shared training utilities</name>
<files>scripts/training_utils.py</files>
<action>
Create `scripts/training_utils.py` by extracting shared logic from `scripts/train_stage1.py`:
1. Extract these functions (with type hints and docstrings):
   - `load_config(config_path: Path) -> dict` — Load YAML config
   - `merge_config(base_config: dict, args: argparse.Namespace) -> dict` — Merge config with CLI overrides
   - `validate_config(config: dict, dry_run: bool = False) -> None` — Validate required fields and file existence
   - `write_run_manifest(config: dict, output_dir: Path) -> Path` — Write run_manifest.json with metadata
   - `set_seeds(seed: int) -> None` — Set deterministic seeds for reproducibility

2. Update `scripts/train_stage1.py` to import from `training_utils`:
   - Replace inline functions with imports: `from scripts.training_utils import load_config, merge_config, validate_config, write_run_manifest, set_seeds`
   - Remove duplicate function definitions from train_stage1.py

3. Follow AGENTS.md patterns:
   - Import order: standard library → third-party → local imports
   - Type hints on all function signatures
   - Error handling with explicit messages and context
</action>
<verify>
<automated>python -c "from scripts.training_utils import load_config, merge_config, validate_config, write_run_manifest, set_seeds; print('OK')"</automated>
</verify>
<done>
- `scripts/training_utils.py` exists with 5 exported functions
- `scripts/train_stage1.py` imports from `training_utils` (no duplicate definitions)
- Import test passes without errors
</done>
</task>

<task type="auto">
<name>Task 2: Implement Stage 2 training CLI</name>
<files>scripts/train_stage2.py</files>
<action>
Create `scripts/train_stage2.py` following D-06 (separate CLI) and D-07 (shared utils):

1. CLI arguments (per D-01, D-06):
   - `--checkpoint` (required): Path to Stage 1 epoch directory — fail if missing or invalid
   - `--config`: Default to `config/training/stage2.yaml`
   - `--dry-run`: Validate config without torch imports
   - Other args: `--output-dir`, `--epochs`, `--batch-size`, `--learning-rate`, `--seed`, etc.

2. Checkpoint validation (per D-02):
   - Check file existence: `model.safetensors` or `pytorch_model.bin`, `tokenizer.json`
   - Run one forward pass test before training starts
   - Fail with clear error if checkpoint invalid

3. Training loop (adapted from train_stage1.py):
   - Load Stage 1 checkpoint: `AutoModelForSeq2SeqLM.from_pretrained(checkpoint_path)`
   - Use Stage 2 config: LR 5e-5, 5 epochs, gradient_accumulation=8
   - Per-epoch checkpointing: `output_dir/epoch_N/` with model, tokenizer, training_state.pt
   - Metrics logging to `train_metrics.jsonl`

4. Import shared utilities:
   ```python
   from scripts.training_utils import (
       load_config, merge_config, validate_config, 
       write_run_manifest, set_seeds
   )
   ```

5. Error handling:
   - `--checkpoint` required: raise ValueError if missing
   - Checkpoint validation fails: raise FileNotFoundError with details
   - Follow AGENTS.md error handling patterns
</action>
<verify>
<automated>python scripts/train_stage2.py --dry-run --checkpoint models/stage1_test --output-dir outputs/stage2_test 2>&1 | grep -E "(DRY-RUN|ERROR|ValidationError)"</automated>
</verify>
<done>
- `scripts/train_stage2.py` exists and is executable
- Missing `--checkpoint` raises ValueError
- Invalid checkpoint path raises FileNotFoundError
- Dry-run mode completes without torch imports
</done>
</task>
</tasks>

<verification>
1. Both scripts exist and are syntactically valid Python
2. `scripts/train_stage1.py` imports from `training_utils` (no duplicate functions)
3. `scripts/train_stage2.py --dry-run` completes successfully
4. Stage 2 training uses Stage 2 config defaults (LR 5e-5, 5 epochs)
</verification>

<success_criteria>
- [ ] Shared utilities extracted and imported by both Stage 1 and Stage 2 scripts
- [ ] Stage 2 CLI accepts `--checkpoint` flag and validates Stage 1 checkpoint
- [ ] Dry-run mode works without torch imports
- [ ] Training loop structure matches Stage 1 pattern (per-epoch checkpoints, metrics logging)
</success_criteria>

<output>
After completion, create `.planning/phases/05-stage-2-domain-adaptation/05-01-SUMMARY.md`
</output>
