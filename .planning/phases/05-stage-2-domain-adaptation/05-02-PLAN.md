---
phase: 05-stage-2-domain-adaptation
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: [scripts/evaluate_stage2.py, scripts/forgetting_detection.py]
autonomous: true
requirements: [TRN2-02]
must_haves:
  truths:
    - "User can evaluate Stage 2 model and compare to Stage 1 baseline"
    - "Forgetting detection runs Stage 2 model on Stage 1 validation samples"
    - "Comparison report shows delta metrics and forgetting warnings"
  artifacts:
    - path: scripts/forgetting_detection.py
      provides: Forgetting detection by comparing Stage 2 vs Stage 1 on Stage 1 val data
      exports: [compute_forgetting_baseline, detect_forgetting]
    - path: scripts/evaluate_stage2.py
      provides: Stage 2 evaluation CLI with comparison reporting
      exports: [main]
    - path: outputs/stage2/stage2_comparison.json
      provides: Comparison metrics artifact
      contains: "stage1_bleu, stage1_chrf, stage2_bleu, stage2_chrf, delta_bleu, delta_chrf, forgetting_bleu, forgetting_delta"
  key_links:
    - from: scripts/evaluate_stage2.py
      to: scripts/forgetting_detection.py
      via: import
      pattern: "from scripts.forgetting_detection import"
    - from: scripts/forgetting_detection.py
      to: data/stage1_val.csv
      via: load_stage_data(stage=1, split="val")
      pattern: "load_stage_data.*stage.*1"
---
<objective>
Implement Stage 2 evaluation with forgetting detection and comparison reporting.

Purpose: Enable measurement of domain adaptation gains while detecting catastrophic forgetting on Stage 1 knowledge.

Output:
- `scripts/forgetting_detection.py` — Forgetting detection utilities
- `scripts/evaluate_stage2.py` — Stage 2 evaluation CLI with comparison reporting
- `outputs/stage2/stage2_comparison.json` — Comparison metrics artifact
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-stage-2-domain-adaptation/05-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@scripts/evaluate_stage1.py
@scripts/train_stage2.py
@AGENTS.md
</context>

<tasks>
<task type="auto">
<name>Task 1: Implement forgetting detection utilities</name>
<files>scripts/forgetting_detection.py</files>
<action>
Create `scripts/forgetting_detection.py` implementing D-03, D-04, D-05:

1. `compute_forgetting_baseline(stage1_checkpoint: Path, val_csv: Path, output_dir: Path) -> dict`:
   - Load Stage 1 checkpoint model
   - Load Stage 1 validation data (stage1_val.csv or use stage3_val.csv subset)
   - Compute BLEU and chrF scores on Stage 1 val
   - Save baseline to `{output_dir}/stage2_forgetting_baseline.json`:
     ```json
     {
       "stage1_checkpoint": "models/stage1_final/epoch_10",
       "bleu": 25.3,
       "chrf": 45.2,
       "computed_at": "2026-03-23T..."
     }
     ```
   - Return baseline metrics dict

2. `detect_forgetting(stage2_checkpoint: Path, baseline: dict, val_csv: Path) -> dict`:
   - Load Stage 2 checkpoint model
   - Evaluate on same validation samples
   - Compute delta: `delta_bleu = stage2_bleu - baseline_bleu`
   - If BLEU drops >2 points (D-05): print warning (not fail)
   - Return forgetting metrics: `{forgetting_bleu, forgetting_delta, stage2_bleu, stage1_bleu}`

3. Helper functions:
   - `load_baseline(output_dir: Path) -> dict` — Load cached baseline
   - `format_forgetting_report(forgetting: dict) -> str` — Human-readable report

4. Use sacrebleu for BLEU, chrf for chrF (reuse from evaluate_stage1.py)

5. Follow AGENTS.md error handling: explicit messages, context in errors
</action>
<verify>
<automated>python -c "from scripts.forgetting_detection import compute_forgetting_baseline, detect_forgetting, load_baseline, format_forgetting_report; print('OK')"</automated>
</verify>
<done>
- `scripts/forgetting_detection.py` exists with 4 exported functions
- Import test passes without errors
- Baseline computation testable on Stage 1 checkpoint
</done>
</task>

<task type="auto">
<name>Task 2: Implement Stage 2 evaluation CLI</name>
<files>scripts/evaluate_stage2.py</files>
<action>
Create `scripts/evaluate_stage2.py` following D-08 (separate CLI) and D-09 (comparison artifact):

1. CLI arguments:
   - `--checkpoint` (required): Path to Stage 2 checkpoint directory
   - `--stage1-checkpoint` (required): Path to Stage 1 checkpoint for comparison
   - `--output-dir` (required): Directory to write `stage2_comparison.json`
   - `--val-csv`: Override validation CSV (default: data/stage3_val.csv)
   - `--dry-run`: Write zero metrics without torch

2. Evaluation flow:
   - Load Stage 2 checkpoint and evaluate on val_csv → `stage2_bleu`, `stage2_chrf`
   - Load Stage 1 checkpoint and evaluate on val_csv → `stage1_bleu`, `stage1_chrf`
   - Compute deltas: `delta_bleu = stage2_bleu - stage1_bleu`, etc.
   - Call `forgetting_detection.detect_forgetting()` for forgetting metrics

3. Write `stage2_comparison.json` (per D-09):
   ```json
   {
     "stage1_bleu": 25.3,
     "stage1_chrf": 45.2,
     "stage2_bleu": 27.1,
     "stage2_chrf": 46.8,
     "delta_bleu": 1.8,
     "delta_chrf": 1.6,
     "forgetting_bleu": 24.9,
     "forgetting_delta": -0.4,
     "checkpoint_path": "models/stage2_final/epoch_5",
     "computed_at": "2026-03-23T..."
   }
   ```

4. Print summary report:
   ```
   Stage 2 Evaluation Complete
   ===========================
   Stage 1 Baseline: BLEU=25.3, chrF=45.2
   Stage 2 Result:   BLEU=27.1, chrF=46.8
   Improvement:      BLEU=+1.8, chrF=+1.6
   
   Forgetting Check: -0.4 BLEU on Stage 1 val (OK - within threshold)
   ```

5. Import shared utilities:
   ```python
   from scripts.training_utils import load_config, merge_config, validate_config, set_seeds
   from scripts.forgetting_detection import compute_forgetting_baseline, detect_forgetting
   ```
</action>
<verify>
<automated>python scripts/evaluate_stage2.py --dry-run --checkpoint models/stage2_test --stage1-checkpoint models/stage1_test --output-dir outputs/stage2_eval 2>&1 | grep -E "(DRY-RUN|ERROR|stage2_comparison.json)"</automated>
</verify>
<done>
- `scripts/evaluate_stage2.py` exists and is executable
- Missing required args raise ValueError
- Dry-run completes without torch imports
- Full run writes `stage2_comparison.json` with all required fields
</done>
</task>
</tasks>

<verification>
1. Both scripts exist and are syntactically valid Python
2. `scripts/evaluate_stage2.py` imports from `scripts.forgetting_detection`
3. `scripts/evaluate_stage2.py --dry-run` completes successfully
4. Evaluation produces `stage2_comparison.json` with all required fields
5. Forgetting warning triggers when BLEU drops >2 points
</verification>

<success_criteria>
- [ ] Forgetting detection computes baseline and detects forgetting on Stage 1 samples
- [ ] Stage 2 evaluation compares Stage 2 vs Stage 1 on same validation set
- [ ] `stage2_comparison.json` artifact contains all required fields (D-09)
- [ ] Forgetting warning (>2 BLEU drop) prints but doesn't fail training
- [ ] CLI follows same pattern as evaluate_stage1.py (dry-run, config overrides)
</success_criteria>

<output>
After completion, create `.planning/phases/05-stage-2-domain-adaptation/05-02-SUMMARY.md`
</output>
