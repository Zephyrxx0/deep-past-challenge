---
phase: 07-evaluation-and-glossary-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/evaluate.py
  - scripts/evaluation_utils.py
  - tests/phase7/test_evaluation.py
autonomous: true
requirements:
  - EVAL-01
  - EVAL-02
must_haves:
  truths:
    - User can compute BLEU and chrF for any checkpoint with CLI
    - User can compare metrics across multiple stages
    - User can compute genre-specific performance breakdown
  artifacts:
    - path: scripts/evaluate.py
      provides: Unified evaluation CLI for all stages
    - path: scripts/evaluation_utils.py
      provides: Shared evaluation utilities (BLEU, chrF, genre metrics)
    - path: tests/phase7/test_evaluation.py
      provides: Test coverage for evaluation functions
      min_lines: 50
  key_links:
    - from: scripts/evaluate.py
      to: scripts/checkpoint_selection.py
      via: reuses evaluate_checkpoint patterns
    - from: scripts/evaluate.py
      to: data/stage3_val.csv
      via: loads validation data with genre tags
---

<objective>
Implement unified evaluation CLI with BLEU, chrF, and genre-specific metrics.

Purpose: Satisfy EVAL-01 and EVAL-02 by providing reproducible command-line evaluation with genre breakdown.
Output: `scripts/evaluate.py` CLI and supporting utilities.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create evaluation utilities and tests</name>
  <files>scripts/evaluation_utils.py, tests/phase7/__init__.py, tests/phase7/test_evaluation.py</files>
  <read_first>scripts/checkpoint_selection.py, scripts/evaluate_stage1.py</read_first>
  <behavior>
    - Test 1: compute_bleu() returns float in [0, 100] range
    - Test 2: compute_chrf() returns float in [0, 100] range
    - Test 3: compute_genre_metrics() returns dict with per-genre BLEU scores
    - Test 4: load_predictions() reads CSV with id, prediction columns
  </behavior>
  <action>
    Create scripts/evaluation_utils.py with:
    
    1. **compute_bleu(predictions, references) -> float**
       - Use sacrebleu library for reproducible BLEU
       - Return score in 0-100 range
    
    2. **compute_chrf(predictions, references) -> float**
       - Use sacrebleu chrF implementation
       - Return score in 0-100 range
    
    3. **compute_genre_metrics(predictions, references, genres) -> Dict[str, Dict]**
       - Group by genre tag ([LETTER], [ADMIN], etc.)
       - Compute BLEU and chrF for each genre
       - Return dict: {genre: {bleu: x, chrf: y, count: n}}
    
    4. **load_predictions(csv_path) -> DataFrame**
       - Load CSV with id, prediction columns
       - Validate schema
    
    Create tests/phase7/test_evaluation.py with tests for each function.
  </action>
  <verify>
    <automated>python -m pytest tests/phase7/test_evaluation.py -xvs</automated>
  </verify>
  <done>Evaluation utilities implemented and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Implement unified evaluation CLI</name>
  <files>scripts/evaluate.py</files>
  <read_first>scripts/evaluation_utils.py, scripts/evaluate_stage3.py</read_first>
  <action>
    Create scripts/evaluate.py CLI with:
    
    1. **CLI Arguments:**
       - --checkpoint: Path to model checkpoint directory
       - --val-data: Path to validation CSV (default: data/stage3_val.csv)
       - --output: Path to output JSON for results
       - --batch-size: Batch size for inference (default: 16)
       - --device: cuda/cpu (default: auto-detect)
       - --genre-breakdown: Enable genre-specific metrics
       - --dry-run: Validate inputs without running evaluation
    
    2. **Evaluation Pipeline:**
       - Load model and tokenizer from checkpoint
       - Load validation data
       - Generate predictions in batches
       - Compute overall BLEU and chrF
       - If --genre-breakdown, compute per-genre metrics
       - Save results to JSON
       - Print formatted results to console
    
    3. **Output Format:**
       ```json
       {
         "checkpoint": "models/stage3_final",
         "val_data": "data/stage3_val.csv",
         "overall": {"bleu": 25.4, "chrf": 48.2, "samples": 500},
         "by_genre": {
           "LETTER": {"bleu": 28.1, "chrf": 51.3, "count": 120},
           "ADMIN": {"bleu": 22.3, "chrf": 45.1, "count": 180}
         }
       }
       ```
    
    Follow patterns from evaluate_stage3.py and checkpoint_selection.py.
  </action>
  <verify>
    <automated>python scripts/evaluate.py --help 2>&1 | grep -q "checkpoint"</automated>
  </verify>
  <done>Unified evaluation CLI complete with genre breakdown support.</done>
</task>

</tasks>

<verification>
1. Run tests: `python -m pytest tests/phase7/test_evaluation.py -xvs`
2. Test CLI: `python scripts/evaluate.py --help`
3. Test dry-run: `python scripts/evaluate.py --checkpoint models/stage3_final --dry-run`
</verification>

<success_criteria>
1. BLEU and chrF computed reproducibly for any checkpoint
2. Genre-specific metrics available with --genre-breakdown flag
3. Results saved to JSON for downstream analysis
4. Requirements EVAL-01 and EVAL-02 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/07-evaluation-and-glossary-quality/07-01-SUMMARY.md`
</output>
