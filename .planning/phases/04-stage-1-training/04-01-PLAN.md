---
phase: 04-stage-1-training
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/train_stage1.py
  - tests/phase4/__init__.py
  - tests/phase4/test_stage1_training.py
autonomous: true
requirements:
  - TRN1-01
must_haves:
  truths:
    - "User can run Stage 1 training from config and complete at least one epoch without errors."
    - "Stage 1 training writes resumable checkpoints and a run manifest into the configured output directory."
    - "Training logs capture per-epoch loss in a structured artifact for later inspection."
  artifacts:
    - path: scripts/train_stage1.py
      provides: Stage 1 training CLI with checkpointing and run manifest output.
    - path: tests/phase4/test_stage1_training.py
      provides: TRN1-01 contract tests covering dry-run validation and artifact creation.
  key_links:
    - from: scripts/train_stage1.py
      to: config/training/stage1.yaml
      via: config load/override merge
      pattern: "stage1\\.yaml"
    - from: scripts/train_stage1.py
      to: scripts/create_dataloader.py
      via: create_dataloader(stage=1, model_id=...)
      pattern: "create_dataloader\\("
    - from: scripts/train_stage1.py
      to: models/stage1_final
      via: checkpoint and manifest write
      pattern: "output_dir"
---

<objective>
Implement a runnable Stage 1 training command that produces resumable checkpoints and structured run artifacts.

Purpose: Satisfy TRN1-01 by enabling a reproducible Stage 1 training run that can be resumed and audited.
Output: Stage 1 training CLI, dry-run validation tests, checkpoint + manifest artifacts.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@config/training/stage1.yaml
@scripts/print_stage_config.py
@scripts/create_dataloader.py
@scripts/data_loader.py
@scripts/check_data_integrity.py
@scripts/model_smoke_test.py

<interfaces>
From scripts/create_dataloader.py:
```python
def create_dataloader(
    stage: int,
    model_id: str,
    batch_size: int = DEFAULT_BATCH_SIZE,
    split: str = "train",
    max_source_length: int = DEFAULT_MAX_SOURCE_LENGTH,
    max_target_length: int = DEFAULT_MAX_TARGET_LENGTH,
    seed: int = SEED,
) -> DataLoader:
    ...
```

From scripts/data_loader.py:
```python
def load_stage_data(stage: int, split: str = "train", seed: int = SEED) -> pd.DataFrame:
    """Returns DataFrame with transliteration_normalized and translation_normalized columns."""
```

From scripts/check_data_integrity.py:
```python
def check_provenance(data_dir: Path = DATA_DIR) -> bool: ...
def check_split_integrity(data_dir: Path = DATA_DIR) -> bool: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add TRN1-01 training CLI dry-run contract tests</name>
  <files>tests/phase4/__init__.py, tests/phase4/test_stage1_training.py</files>
  <behavior>
    - Test 1: `python scripts/train_stage1.py --dry-run --output-dir <tmp>` exits 0 and writes run_manifest.json with stage=1, model_id, seed.
    - Test 2: Invalid config override (missing train_csv) exits non-zero with an error message containing "train_csv".
  </behavior>
  <action>
    Create Phase 4 unittest scaffold and CLI contract tests for Stage 1 training. Use tempfile directories for output and CSV fixtures created at test runtime. Execute the CLI via subprocess with `--dry-run` so tests remain lightweight and deterministic even without torch installed. Assert manifest JSON keys and non-zero exit behavior on invalid overrides. Ensure tests reference fixed-seed expectations per AGENTS.md (SEED=42).
  </action>
  <verify>
    <automated>python -m unittest tests.phase4.test_stage1_training -v</automated>
  </verify>
  <done>Dry-run contract tests exist and pass, enforcing Stage 1 CLI validation and artifact creation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Stage 1 training CLI with checkpointing and run manifests</name>
  <files>scripts/train_stage1.py</files>
  <behavior>
    - Test 1: Dry-run writes run_manifest.json and exits 0 without importing torch.
    - Test 2: Invalid overrides return non-zero with actionable error text.
  </behavior>
  <action>
    Implement `scripts/train_stage1.py` with argparse. Load `config/training/stage1.yaml` via YAML (use defaults) and allow overrides for `--train-csv`, `--val-csv`, `--model-id`, `--output-dir`, `--epochs`, `--batch-size`, `--learning-rate`, `--seed`, `--max-source-length`, and `--max-target-length`. Add `--dry-run` to validate config/paths and write `run_manifest.json` (stage, model_id, train_csv, val_csv, seed, output_dir, started_at_utc) without torch imports. Enforce default SEED=42 per AGENTS.md unless overridden.

    For non-dry runs: set deterministic seeds (random, numpy, torch, PYTHONHASHSEED), run `check_provenance()` and `check_split_integrity()` before training, then build the training loader using `create_dataloader(stage=1, model_id=..., split="train")`. (Validation is handled by the Stage 1 evaluation CLI in Plan 04-02; do not attempt to load a Stage 1 "val" split here.) Load model/tokenizer via `AutoModelForSeq2SeqLM.from_pretrained()` and `AutoTokenizer.from_pretrained()` using `model_id` or `--resume-from` checkpoint path. Implement a minimal training loop with AdamW + linear warmup scheduler, gradient accumulation (from config), and per-epoch loss tracking. After each epoch, write:
    - `output_dir/epoch_{N}/` with `model.save_pretrained(...)` and `tokenizer.save_pretrained(...)`
    - `output_dir/epoch_{N}/training_state.pt` containing epoch, global_step, optimizer state, scheduler state
    - `output_dir/train_metrics.jsonl` appending epoch, train_loss, steps

    Support `--resume-from` by loading model/tokenizer from the checkpoint dir and restoring optimizer/scheduler state if `training_state.pt` exists; resume at next epoch.
  </action>
  <verify>
    <automated>python -m unittest tests.phase4.test_stage1_training -v</automated>
  </verify>
  <done>Stage 1 training CLI can run, produce resumable checkpoints, and write structured run artifacts per TRN1-01.</done>
</task>

</tasks>

<verification>
1. Run `python -m unittest tests.phase4.test_stage1_training -v` and confirm all tests pass.
2. Optional smoke test (when torch available): `python scripts/train_stage1.py --epochs 1 --batch-size 2 --output-dir outputs/stage1_smoke` completes and writes epoch_1 checkpoint + train_metrics.jsonl.
</verification>

<success_criteria>
- TRN1-01 satisfied: Stage 1 training is runnable and produces resumable checkpoints.
- Run manifest and per-epoch metrics are persisted in the configured output directory.
- Dry-run validation provides fast feedback without heavy ML dependencies.
</success_criteria>

<output>
After completion, create `.planning/phases/04-stage-1-training/04-stage-1-training-01-SUMMARY.md`.
</output>
