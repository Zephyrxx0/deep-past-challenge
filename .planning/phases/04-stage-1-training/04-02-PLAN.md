---
phase: 04-stage-1-training
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - scripts/evaluate_stage1.py
  - tests/phase4/test_stage1_evaluation.py
autonomous: true
requirements:
  - TRN1-02
must_haves:
  truths:
    - "User can evaluate Stage 1 checkpoint on validation data and receive BLEU/chrF metrics."
    - "Stage 1 evaluation writes metrics to a structured artifact in the output directory."
  artifacts:
    - path: scripts/evaluate_stage1.py
      provides: Stage 1 evaluation CLI producing BLEU/chrF metrics.
    - path: tests/phase4/test_stage1_evaluation.py
      provides: TRN1-02 contract tests for evaluation outputs.
  key_links:
    - from: scripts/evaluate_stage1.py
      to: models/stage1_final
      via: checkpoint load (AutoModelForSeq2SeqLM/AutoTokenizer)
      pattern: "stage1_final"
    - from: scripts/evaluate_stage1.py
      to: data/stage3_val.csv
      via: validation dataset load
      pattern: "stage3_val\\.csv"
    - from: scripts/evaluate_stage1.py
      to: outputs/stage1_metrics.json
      via: metrics artifact write
      pattern: "metrics"
---

<objective>
Add a Stage 1 evaluation command that computes BLEU/chrF and persists metrics artifacts.

Purpose: Satisfy TRN1-02 by making Stage 1 validation metrics reproducible and machine-readable.
Output: Evaluation CLI + contract tests + metrics artifact format.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@config/training/stage1.yaml
@scripts/create_dataloader.py
@scripts/data_loader.py
@scripts/train_stage1.py
@scripts/resolve_tokenizer.py
@scripts/tokenizer_roundtrip_check.py

<interfaces>
From scripts/create_dataloader.py:
```python
def create_dataloader(stage: int, model_id: str, batch_size: int = DEFAULT_BATCH_SIZE, split: str = "train", max_source_length: int = DEFAULT_MAX_SOURCE_LENGTH, max_target_length: int = DEFAULT_MAX_TARGET_LENGTH, seed: int = SEED) -> DataLoader:
    ...
```

From config/training/stage1.yaml:
```yaml
model_id: facebook/mbart-large-50-many-to-many-mmt
train_csv: data/stage1_train.csv
val_csv: data/stage3_val.csv
epochs: 10
learning_rate: 1e-4
batch_size: 4
gradient_accumulation_steps: 4
warmup_steps: 500
max_source_length: 256
max_target_length: 256
seed: 42
output_dir: models/stage1_final
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add TRN1-02 evaluation contract tests</name>
  <files>tests/phase4/__init__.py, tests/phase4/test_stage1_evaluation.py</files>
  <behavior>
    - Test 1: `python scripts/evaluate_stage1.py --dry-run --output-dir <tmp>` exits 0 and writes stage1_metrics.json with keys [stage, bleu, chrf, samples].
    - Test 2: Missing checkpoint path exits non-zero and prints error containing "checkpoint".
  </behavior>
  <action>
    Create unittest contract tests for the evaluation CLI. Use subprocess to run the CLI with `--dry-run` so tests are lightweight and do not require torch. Assert metrics JSON schema and error handling when checkpoint is missing.
  </action>
  <verify>
    <automated>python -m unittest tests.phase4.test_stage1_evaluation -v</automated>
  </verify>
  <done>Stage 1 evaluation contract tests enforce dry-run metrics artifacts and error handling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Stage 1 evaluation CLI with BLEU/chrF output</name>
  <files>scripts/evaluate_stage1.py</files>
  <behavior>
    - Test 1: Dry-run writes metrics artifact without torch dependencies.
    - Test 2: Missing checkpoint fails with helpful error.
  </behavior>
  <action>
    Implement `scripts/evaluate_stage1.py` with argparse. Load defaults from `config/training/stage1.yaml` and allow overrides for `--checkpoint`, `--val-csv`, `--output-dir`, `--max-source-length`, `--max-target-length`, `--batch-size`, `--seed`, and `--dry-run`. In dry-run mode, write `stage1_metrics.json` containing stage=1, bleu=0.0, chrf=0.0, samples=0, timestamp.

    For real evaluation: load model/tokenizer from checkpoint path, then build a validation dataset by reading `val_csv` with pandas (expected to include `transliteration`/`translation` columns). Construct a minimal in-memory Dataset that yields `transliteration_normalized`/`translation_normalized` values and use the same tokenization logic as `create_dataloader` (mirror `collate_fn` behavior) to batch inputs. Generate translations with `model.generate` (max_new_tokens from config) and compute BLEU/chrF with sacrebleu. Write `stage1_metrics.json` with bleu/chrf scores, samples count, and checkpoint path. Ensure errors surface with `error:` prefix and non-zero exit.
  </action>
  <verify>
    <automated>python -m unittest tests.phase4.test_stage1_evaluation -v</automated>
  </verify>
  <done>Stage 1 evaluation CLI produces BLEU/chrF metrics artifacts for validation data per TRN1-02.</done>
</task>

</tasks>

<verification>
1. Run `python -m unittest tests.phase4.test_stage1_evaluation -v` and confirm all tests pass.
2. Optional full evaluation (when torch installed): `python scripts/evaluate_stage1.py --checkpoint models/stage1_final --output-dir outputs/stage1_eval` writes stage1_metrics.json with non-zero sample count.
</verification>

<success_criteria>
- TRN1-02 satisfied: evaluation produces BLEU/chrF metrics artifacts for Stage 1.
- Metrics output is structured, versioned, and reproducible.
</success_criteria>

<output>
After completion, create `.planning/phases/04-stage-1-training/04-stage-1-training-02-SUMMARY.md`.
</output>
