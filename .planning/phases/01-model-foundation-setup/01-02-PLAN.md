---
phase: 01-model-foundation-setup
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - tests/phase1/test_model_smoke_test.py
  - scripts/model_smoke_test.py
autonomous: true
requirements:
  - ARCH-02
must_haves:
  truths:
    - User can run one command to load model and tokenizer from active config.
    - Smoke command produces one translation and exits successfully.
  artifacts:
    - path: tests/phase1/test_model_smoke_test.py
      provides: Automated checks for smoke command output and exit behavior.
    - path: scripts/model_smoke_test.py
      provides: CLI smoke test that loads model/tokenizer and generates one sample translation.
  key_links:
    - from: scripts/model_smoke_test.py
      to: config/models/active_model.yaml
      via: active model resolution
      pattern: "active_model\.yaml"
    - from: scripts/model_smoke_test.py
      to: transformers
      via: AutoTokenizer and AutoModelForSeq2SeqLM load
      pattern: "AutoTokenizer|AutoModelForSeq2SeqLM"
---

<objective>
Implement a reliable model smoke test command that proves selected model loading and one-step generation.

Purpose: Satisfy ARCH-02 and catch load/tokenizer wiring issues before training scripts are introduced.
Output: Smoke script + automated tests validating command behavior and expected output shape.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-model-foundation-setup/01-RESEARCH.md
@.planning/phases/01-model-foundation-setup/01-01-PLAN.md

<interfaces>
From config/models/active_model.yaml:
```yaml
model_id: facebook/mbart-large-50-many-to-many-mmt
hf_checkpoint: facebook/mbart-large-50-many-to-many-mmt
selected_at_utc: 2026-03-22T00:00:00Z
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ARCH-02 smoke command tests</name>
  <files>tests/phase1/test_model_smoke_test.py</files>
  <read_first>AGENTS.md, config/models/active_model.yaml, .planning/phases/01-model-foundation-setup/01-RESEARCH.md</read_first>
  <behavior>
    - Test 1: Smoke script prints a line prefixed with `translation:` and exits 0 when run with tiny override model.
    - Test 2: Smoke script fails with non-zero exit and readable error when active config path is missing.
    - Test 3: Smoke script accepts `--input-text` and propagates it to generation path (validated by output containing expected command echo line).
  </behavior>
  <action>Create unittest for `scripts/model_smoke_test.py` using `subprocess.run`. Use fast override flags in tests: `--model-id hf-internal-testing/tiny-random-t5` and `--max-new-tokens 12` so tests stay under 60s. Assert output includes exact markers `model_loaded:`, `tokenizer_loaded:`, and `translation:`. Include missing-config negative test using a temporary non-existent path.</action>
  <acceptance_criteria>
    - tests/phase1/test_model_smoke_test.py contains both success and failure-path tests.
    - Tests assert output markers: model_loaded:, tokenizer_loaded:, translation:.
    - Test command exits 0 after Task 2 implementation.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.phase1.test_model_smoke_test -v</automated>
  </verify>
  <done>ARCH-02 smoke behavior is codified in automated tests before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement model smoke test CLI with active-config default</name>
  <files>scripts/model_smoke_test.py</files>
  <read_first>tests/phase1/test_model_smoke_test.py, config/models/active_model.yaml, AGENTS.md</read_first>
  <behavior>
    - Test 1: Default run reads active model config and attempts model+tokenizer load.
    - Test 2: Override `--model-id` bypasses active config selection.
    - Test 3: Successful run prints one translation line and exits 0.
  </behavior>
  <action>Implement `scripts/model_smoke_test.py` with argparse flags: `--active-config` (default `config/models/active_model.yaml`), `--model-id` (optional override), `--input-text` (default `a-na šar-ri be-li-ia`), `--max-new-tokens` (default 32), and `--device` (`auto|cpu|cuda`, default auto). Load checkpoint via `AutoTokenizer.from_pretrained` and `AutoModelForSeq2SeqLM.from_pretrained`, tokenize input, run `model.generate(...)`, decode first output, and print exactly: `model_loaded: ...`, `tokenizer_loaded: ...`, `translation: ...`. Raise clear FileNotFoundError when config missing and exit non-zero with message prefix `error:`.</action>
  <acceptance_criteria>
    - scripts/model_smoke_test.py supports all specified CLI flags.
    - Successful run emits required three output markers.
    - Missing active config path yields non-zero exit and `error:` message.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.phase1.test_model_smoke_test -v</automated>
  </verify>
  <done>User can run a single smoke command to load tokenizer/model and generate one translation sample.</done>
</task>

</tasks>

<verification>
Run `python -m unittest tests.phase1.test_model_smoke_test -v` and verify all ARCH-02 checks pass.
</verification>

<success_criteria>
- ARCH-02 smoke command is runnable and deterministic.
- Smoke command proves end-to-end load + generation path.
- Automated tests cover success and key failure path.
</success_criteria>

<output>
After completion, create `.planning/phases/01-model-foundation-setup/01-model-foundation-setup-02-SUMMARY.md`
</output>
