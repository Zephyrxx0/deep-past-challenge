---
phase: 01-model-foundation-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/phase1/__init__.py
  - tests/phase1/test_model_catalog_contract.py
  - tests/phase1/test_model_selector.py
  - config/models/catalog.yaml
  - config/models/active_model.yaml
  - scripts/select_model.py
autonomous: true
requirements:
  - ARCH-01
must_haves:
  truths:
    - User can choose one approved base model using one CLI command.
    - The selected model persists in a versioned active-model config file.
  artifacts:
    - path: tests/phase1/test_model_catalog_contract.py
      provides: Contract checks for approved model IDs and required fields.
    - path: scripts/select_model.py
      provides: Single-command model selection CLI.
    - path: config/models/catalog.yaml
      provides: Canonical approved model catalog.
    - path: config/models/active_model.yaml
      provides: Persisted current model selection.
  key_links:
    - from: scripts/select_model.py
      to: config/models/catalog.yaml
      via: catalog lookup before accepting model
      pattern: "catalog\.yaml"
    - from: scripts/select_model.py
      to: config/models/active_model.yaml
      via: persisted write of selected model
      pattern: "active_model\.yaml"
---

<objective>
Create a deterministic, single-command model selection baseline for approved Phase 1 checkpoints.

Purpose: Satisfy ARCH-01 with a stable config-driven selector before downstream smoke tests and stage configs consume active model state.
Output: Versioned catalog + active selection config + selector CLI + contract tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-model-foundation-setup/01-RESEARCH.md
@.planning/phases/01-model-foundation-setup/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ARCH-01 contract tests and test package scaffold</name>
  <files>tests/phase1/__init__.py, tests/phase1/test_model_catalog_contract.py, tests/phase1/test_model_selector.py</files>
  <read_first>AGENTS.md, .planning/REQUIREMENTS.md, .planning/phases/01-model-foundation-setup/01-RESEARCH.md</read_first>
  <behavior>
    - Test 1: Catalog contract includes exactly three supported IDs: facebook/mbart-large-50-many-to-many-mmt, google/mt5-base, facebook/nllb-200-distilled-600M.
    - Test 2: Selector command rejects unsupported IDs with non-zero exit and clear error text containing "unsupported model".
    - Test 3: Selector command writes selected model_id into active_model config and exits 0 for supported IDs.
  </behavior>
  <action>Create unittest files for ARCH-01 with concrete assertions for the three required model IDs and selector behavior. Use `subprocess.run([...], capture_output=True, text=True)` to execute `python scripts/select_model.py --model-id ... --output config/models/active_model.yaml` from tests. Keep tests deterministic and independent by writing temporary active_model paths under `tempfile.TemporaryDirectory()` for selector behavior checks.</action>
  <acceptance_criteria>
    - tests/phase1/test_model_catalog_contract.py contains all three exact checkpoint strings.
    - tests/phase1/test_model_selector.py contains one success-path test and one unsupported-model failure test.
    - python -m unittest discover -s tests/phase1 -p "test_model_*.py" -v exits 0 after Task 2 implementation.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest discover -s tests/phase1 -p "test_model_*.py" -v</automated>
  </verify>
  <done>Phase 1 ARCH-01 tests exist, run, and enforce exact approved-model contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement model catalog and selector CLI</name>
  <files>config/models/catalog.yaml, config/models/active_model.yaml, scripts/select_model.py</files>
  <read_first>config/models/catalog.yaml, tests/phase1/test_model_catalog_contract.py, tests/phase1/test_model_selector.py, AGENTS.md</read_first>
  <behavior>
    - Test 1: `--model-id facebook/mbart-large-50-many-to-many-mmt` writes active_model with that ID.
    - Test 2: `--model-id google/mt5-base` writes active_model with that ID.
    - Test 3: Unsupported model value fails with informative message and no successful write confirmation.
  </behavior>
  <action>Create `config/models/catalog.yaml` with an `approved_models` array containing exactly the three Phase 1 model IDs plus human-readable `family` and `hf_checkpoint` fields. Create `config/models/active_model.yaml` defaulting to mBART-50. Implement `scripts/select_model.py` using argparse with required `--model-id` and optional `--catalog` (default `config/models/catalog.yaml`) and `--output` (default `config/models/active_model.yaml`). Validate requested ID exists in catalog, then atomically write active model YAML with keys `model_id`, `hf_checkpoint`, `selected_at_utc`. Exit code 0 on success, 2 on validation failure.</action>
  <acceptance_criteria>
    - config/models/catalog.yaml contains exactly 3 approved_models entries.
    - scripts/select_model.py supports --model-id, --catalog, --output flags.
    - Running selector for each approved ID exits 0 and updates output file model_id.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest discover -s tests/phase1 -p "test_model_*.py" -v</automated>
  </verify>
  <done>Single-command model selection works for all approved checkpoints and persists active selection in config.</done>
</task>

</tasks>

<verification>
Run `python -m unittest discover -s tests/phase1 -p "test_model_*.py" -v` and confirm all ARCH-01 tests pass.
</verification>

<success_criteria>
- ARCH-01 is met by a single selector command and persisted active config.
- Supported model set is locked to the three roadmap-approved checkpoints.
- Selector behavior is guarded by automated contract tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-model-foundation-setup/01-model-foundation-setup-01-SUMMARY.md`
</output>
