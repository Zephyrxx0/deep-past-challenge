---
phase: 01-model-foundation-setup
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - tests/phase1/test_stage_config_loading.py
  - config/training/stage1.yaml
  - config/training/stage2.yaml
  - config/training/stage3.yaml
  - scripts/print_stage_config.py
autonomous: true
requirements:
  - ARCH-03
must_haves:
  truths:
    - User can run a script to load stage-specific training config for stages 1, 2, and 3.
    - Stage configs are versioned files with explicit hyperparameters per stage.
  artifacts:
    - path: tests/phase1/test_stage_config_loading.py
      provides: Automated coverage for stage config keys and loader behavior.
    - path: config/training/stage1.yaml
      provides: Stage 1 hyperparameter config.
    - path: config/training/stage2.yaml
      provides: Stage 2 hyperparameter config.
    - path: config/training/stage3.yaml
      provides: Stage 3 hyperparameter config.
    - path: scripts/print_stage_config.py
      provides: CLI command to load and print chosen stage config.
  key_links:
    - from: scripts/print_stage_config.py
      to: config/training/stage1.yaml
      via: stage-to-file resolution
      pattern: "stage1\.yaml"
    - from: scripts/print_stage_config.py
      to: config/training/stage2.yaml
      via: stage-to-file resolution
      pattern: "stage2\.yaml"
    - from: scripts/print_stage_config.py
      to: config/training/stage3.yaml
      via: stage-to-file resolution
      pattern: "stage3\.yaml"
---

<objective>
Establish versioned, script-loadable Stage 1/2/3 hyperparameter configs.

Purpose: Satisfy ARCH-03 so all later training scripts consume explicit per-stage config instead of duplicated hardcoded values.
Output: Stage config files + loader CLI + automated contract tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-model-foundation-setup/01-RESEARCH.md
@.planning/research/model_architectures.md

<interfaces>
Required keys each stage config must include:
```text
model_id, train_csv, val_csv, epochs, learning_rate, batch_size,
gradient_accumulation_steps, warmup_steps, max_source_length,
max_target_length, seed, output_dir
```

Learning rate values (from research):
```text
stage1: 1e-4
stage2: 5e-5
stage3: 1e-5
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ARCH-03 stage config contract tests</name>
  <files>tests/phase1/test_stage_config_loading.py</files>
  <read_first>AGENTS.md, .planning/phases/01-model-foundation-setup/01-RESEARCH.md, .planning/research/model_architectures.md</read_first>
  <behavior>
    - Test 1: Stage 1/2/3 config files exist and each includes required keys.
    - Test 2: Learning-rate values exactly match 1e-4, 5e-5, and 1e-5 by stage.
    - Test 3: Loader CLI prints JSON containing selected stage and config payload.
  </behavior>
  <action>Create unittest file that loads YAML files from `config/training/` using `yaml.safe_load`, validates required keys, and asserts exact learning-rate values per stage. Add subprocess-based test for `python scripts/print_stage_config.py --stage stage2` asserting stdout includes `"stage": "stage2"` and `"learning_rate": 5e-05` or equivalent JSON float representation.</action>
  <acceptance_criteria>
    - tests/phase1/test_stage_config_loading.py checks all three files and key presence.
    - Tests enforce exact phase LR schedule values.
    - Loader CLI output format is machine-readable JSON.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.phase1.test_stage_config_loading -v</automated>
  </verify>
  <done>ARCH-03 expectations are explicitly encoded in tests before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement stage config files and config loader CLI</name>
  <files>config/training/stage1.yaml, config/training/stage2.yaml, config/training/stage3.yaml, scripts/print_stage_config.py</files>
  <read_first>tests/phase1/test_stage_config_loading.py, AGENTS.md, .planning/ROADMAP.md</read_first>
  <behavior>
    - Test 1: Each stage file is valid YAML and includes required training keys.
    - Test 2: Stage-specific learning rates match roadmap/research schedule.
    - Test 3: CLI command selects stage file by `--stage` and prints JSON object.
  </behavior>
  <action>Create three YAML files under `config/training/` with shared schema and concrete stage-specific values: (a) stage1 uses `data/stage1_train.csv`, `epochs: 10`, `learning_rate: 1e-4`; (b) stage2 uses `data/stage2_train.csv`, `epochs: 5`, `learning_rate: 5e-5`; (c) stage3 uses `data/stage3_train.csv`, `val_csv: data/stage3_val.csv`, `epochs: 10`, `learning_rate: 1e-5`. Set `seed: 42` for all. Implement `scripts/print_stage_config.py` with `--stage {stage1,stage2,stage3}` and optional `--config-dir` (default `config/training`) to load YAML and print JSON `{"stage":...,"config":...}`. Return exit code 2 for invalid stage.</action>
  <acceptance_criteria>
    - config/training/stage{1,2,3}.yaml exist and load with yaml.safe_load.
    - print_stage_config CLI resolves each stage and prints JSON payload.
    - All ARCH-03 tests pass via unittest command.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.phase1.test_stage_config_loading -v</automated>
  </verify>
  <done>Stage-specific hyperparameters are versioned and script-callable for all three stages.</done>
</task>

</tasks>

<verification>
Run `python -m unittest tests.phase1.test_stage_config_loading -v` and verify stage config tests pass.
</verification>

<success_criteria>
- ARCH-03 is met with versioned stage config files and CLI loader.
- Phase-specific LR progression is encoded and validated.
- Future training scripts can consume a single stable config interface.
</success_criteria>

<output>
After completion, create `.planning/phases/01-model-foundation-setup/01-model-foundation-setup-03-SUMMARY.md`
</output>
