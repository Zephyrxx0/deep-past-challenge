---
phase: 03-data-pipeline-and-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/data_loader.py
  - tests/phase3/test_data_loader.py
  - tests/phase3/__init__.py
autonomous: true
requirements: [DATA-01]

must_haves:
  truths:
    - "User can load stage1_train.csv with deterministic row order across runs"
    - "User can load stage2_train.csv with deterministic row order across runs"
    - "User can load stage3_train.csv and stage3_val.csv with deterministic row order across runs"
    - "Loaded datasets contain normalized transliteration and translation columns"
  artifacts:
    - path: "scripts/data_loader.py"
      provides: "Stage dataset loader with fixed-seed sampling"
      min_lines: 80
    - path: "tests/phase3/test_data_loader.py"
      provides: "DATA-01 contract tests for deterministic loading"
      exports: ["TestDataLoader"]
  key_links:
    - from: "scripts/data_loader.py"
      to: "data/stage*.csv"
      via: "pandas.read_csv with fixed seed"
      pattern: "pd\\.read_csv.*seed"
---

<objective>
Build deterministic dataset loaders for all 3-stage training CSVs with reproducible sampling and validation.

**Purpose:** Ensure training runs are reproducible across machines and environments by enforcing fixed-seed data loading with contract tests.

**Output:** 
- CLI command to load any stage dataset with deterministic ordering
- Contract tests validating DATA-01 requirement
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@AGENTS.md
@data.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add DATA-01 deterministic loading contract tests</name>
  <files>tests/phase3/__init__.py, tests/phase3/test_data_loader.py</files>
  
  <read_first>
- tests/phase1/test_model_selector.py (established test patterns from Phase 1)
- tests/phase2/test_tokenizer_training.py (established test patterns from Phase 2)
- data.md (dataset structure and columns)
- AGENTS.md (reproducibility section with SEED=42 convention)
  </read_first>
  
  <behavior>
- Test 1: Loading stage1_train.csv twice with same seed produces identical row order (compare first 10 row indices)
- Test 2: Loading stage2_train.csv twice with same seed produces identical row order
- Test 3: Loading stage3_train.csv twice with same seed produces identical row order
- Test 4: Loading stage3_val.csv twice with same seed produces identical row order
- Test 5: Loaded DataFrames contain required columns: transliteration_normalized, translation_normalized
- Test 6: CLI command `python scripts/data_loader.py --stage 1 --split train` exits 0 and outputs valid JSON with row_count key
  </behavior>
  
  <action>
Create tests/phase3/__init__.py as empty package marker.

Create tests/phase3/test_data_loader.py with unittest.TestCase class TestDataLoader:
1. Test method test_stage1_deterministic_loading that calls load_stage_data(stage=1, split='train', seed=42) twice and asserts df1.index.equals(df2.index) and first 10 transliteration values match
2. Test method test_stage2_deterministic_loading (same pattern for stage=2)
3. Test method test_stage3_train_deterministic_loading (same pattern for stage=3, split='train')
4. Test method test_stage3_val_deterministic_loading (same pattern for stage=3, split='val')
5. Test method test_required_columns that loads each stage and asserts 'transliteration_normalized' in df.columns and 'translation_normalized' in df.columns
6. Test method test_cli_command that runs subprocess calling scripts/data_loader.py with --stage 1 --split train, asserts returncode == 0, and JSON output contains "row_count" key

Import statements: import unittest, import subprocess, import json, import pandas as pd, from scripts.data_loader import load_stage_data (will fail initially - this is RED phase).

Follow AGENTS.md conventions: SEED=42, snake_case naming, 4-space indentation.
  </action>
  
  <verify>
    <automated>python -m unittest tests.phase3.test_data_loader -v 2>&1 | grep -E "(FAIL|ERROR)" && echo "Tests failing as expected (RED phase)" || echo "ERROR: Tests should fail before implementation"</automated>
  </verify>
  
  <acceptance_criteria>
- tests/phase3/__init__.py exists and is empty (0 bytes)
- tests/phase3/test_data_loader.py contains class TestDataLoader(unittest.TestCase)
- test_data_loader.py contains 6 test methods: test_stage1_deterministic_loading, test_stage2_deterministic_loading, test_stage3_train_deterministic_loading, test_stage3_val_deterministic_loading, test_required_columns, test_cli_command
- test_data_loader.py imports: unittest, subprocess, json, pandas, scripts.data_loader.load_stage_data
- All test methods use SEED=42 constant
- Running `python -m unittest tests.phase3.test_data_loader` produces failures (RED phase - expected before implementation)
  </acceptance_criteria>
  
  <done>
DATA-01 contract tests exist and fail initially (RED phase of TDD), establishing the specification for deterministic data loading.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic stage data loader CLI and library</name>
  <files>scripts/data_loader.py</files>
  
  <read_first>
- tests/phase3/test_data_loader.py (tests define the contract)
- data.md (CSV file paths and column structure)
- config/training/stage1.yaml (train_csv paths)
- config/training/stage2.yaml (train_csv paths)
- config/training/stage3.yaml (train_csv and val_csv paths)
- AGENTS.md (SEED=42 convention, error handling patterns)
- scripts/train_tokenizer.py (established CLI pattern from Phase 2)
  </read_first>
  
  <action>
Create scripts/data_loader.py with:

1. **Imports** (follow AGENTS.md import order):
   - Standard: import argparse, import json, import sys, from pathlib import Path
   - Third-party: import pandas as pd, import numpy as np
   - Local: (none needed)

2. **Constants**:
   - SEED = 42
   - STAGE_CONFIGS = {
       1: {'train': 'data/stage1_train.csv'},
       2: {'train': 'data/stage2_train.csv'},
       3: {'train': 'data/stage3_train.csv', 'val': 'data/stage3_val.csv'}
     }

3. **Function load_stage_data(stage: int, split: str = 'train', seed: int = SEED) -> pd.DataFrame:**
   - Validate stage in [1, 2, 3], raise ValueError otherwise
   - Validate split in STAGE_CONFIGS[stage], raise ValueError if invalid split for stage
   - Get csv_path from STAGE_CONFIGS[stage][split]
   - Check Path(csv_path).exists(), raise FileNotFoundError with message "Dataset not found at {csv_path}. Run data preprocessing first." if missing
   - Set random seeds: np.random.seed(seed), pd.options.mode.chained_assignment = None
   - Load: df = pd.read_csv(csv_path)
   - Validate required columns exist: assert 'transliteration_normalized' in df.columns and 'translation_normalized' in df.columns, "Dataset missing normalized columns"
   - Reset index to ensure deterministic ordering: df = df.reset_index(drop=True)
   - Return df

4. **Function main():**
   - Create ArgumentParser with description "Load stage datasets with deterministic seed"
   - Add arguments: --stage (type=int, required=True, choices=[1,2,3]), --split (type=str, default='train'), --seed (type=int, default=SEED)
   - Parse args
   - Call load_stage_data(args.stage, args.split, args.seed)
   - Output JSON to stdout: json.dumps({'row_count': len(df), 'columns': list(df.columns), 'stage': args.stage, 'split': args.split})
   - Handle exceptions: catch ValueError/FileNotFoundError, print to stderr, sys.exit(1)

5. **Entry point**: if __name__ == '__main__': main()

Follow AGENTS.md conventions: snake_case, 4-space indent, type hints, explicit error messages, single quotes for strings.
  </action>
  
  <verify>
    <automated>python -m unittest tests.phase3.test_data_loader -v</automated>
  </verify>
  
  <acceptance_criteria>
- scripts/data_loader.py exists and contains function load_stage_data with signature (stage: int, split: str = 'train', seed: int = SEED) -> pd.DataFrame
- data_loader.py contains SEED = 42 constant
- data_loader.py contains STAGE_CONFIGS dict mapping stages to CSV paths matching data/ directory structure
- load_stage_data validates stage in [1, 2, 3] and raises ValueError for invalid stages
- load_stage_data validates split exists for the stage and raises ValueError for invalid splits
- load_stage_data checks file existence and raises FileNotFoundError with helpful message
- load_stage_data sets np.random.seed(seed) before loading
- load_stage_data validates presence of transliteration_normalized and translation_normalized columns
- main() function accepts --stage, --split, --seed arguments and outputs JSON
- Running `python scripts/data_loader.py --stage 1 --split train` exits 0 and outputs valid JSON
- Running `python -m unittest tests.phase3.test_data_loader -v` produces 6 passed tests (GREEN phase)
  </acceptance_criteria>
  
  <done>
DATA-01 satisfied: deterministic data loading works for all stages with fixed-seed reproducibility, validated by passing contract tests.
  </done>
</task>

</tasks>

<verification>
1. Run full test suite: `python -m unittest tests.phase3.test_data_loader -v` → all tests pass
2. Manual smoke test: `python scripts/data_loader.py --stage 3 --split val` → outputs JSON with row_count
3. Determinism check: Run load command twice, compare output JSON → row_count identical
4. Requirement check: DATA-01 fully addressed (deterministic loaders for all stage CSVs with fixed-seed reproducibility)
</verification>

<success_criteria>
- [ ] DATA-01 requirement satisfied
- [ ] All 6 contract tests pass
- [ ] CLI command works for all valid stage/split combinations
- [ ] Deterministic: same seed produces identical row order across runs
- [ ] Required columns validated on load
- [ ] Helpful error messages for missing files or invalid inputs
- [ ] Code follows AGENTS.md conventions (snake_case, SEED=42, type hints, error handling)
</success_criteria>

<output>
After completion, create `.planning/phases/03-data-pipeline-and-integrity/03-01-SUMMARY.md`
</output>
