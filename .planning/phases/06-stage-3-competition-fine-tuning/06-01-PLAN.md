---
phase: 06-stage-3-competition-fine-tuning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: 
  - scripts/train_stage3.py
  - scripts/training_utils.py
  - tests/test_stage3_training.py
autonomous: true
requirements: [TRN3-01]
user_setup: []

must_haves:
  truths:
    - "User can train Stage 3 model from Stage 2 checkpoint with genre-conditioned inputs"
    - "Early stopping triggers based on validation BLEU monitoring"
    - "Per-epoch checkpoints saved with complete training state"
  artifacts:
    - path: "scripts/train_stage3.py"
      provides: "Stage 3 training CLI with early stopping"
      exports: ["main", "parse_args", "train_stage3"]
    - path: "scripts/training_utils.py"
      provides: "Updated utilities with early stopping support"
      contains: ["EarlyStopping", "should_stop_early"]
    - path: "tests/test_stage3_training.py"
      provides: "Test coverage for Stage 3 training features"
      min_lines: 30
  key_links:
    - from: "scripts/train_stage3.py"
      to: "config/training/stage3.yaml"
      via: "config loading with CLI overrides"
      pattern: "load_config.*stage3.yaml"
    - from: "scripts/train_stage3.py"
      to: "scripts/train_stage2.py"
      via: "inherits training pattern but adds early stopping"
      pattern: "early.*stop"
    - from: "scripts/train_stage3.py"
      to: "data/stage3_train.csv"
      via: "data loading with genre tag validation"
      pattern: "stage3_train"
---

<objective>
Implement Stage 3 training CLI with genre-conditioned inputs and early stopping.

Purpose: Enable fine-tuning for competition domain with careful validation monitoring to prevent overfitting.
Output: Stage 3 training script that loads from Stage 2 checkpoint, trains with genre tags, implements early stopping, and saves per-epoch checkpoints.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# Reference Phase 4/5 training patterns
@scripts/train_stage1.py
@scripts/train_stage2.py
@scripts/training_utils.py
@config/training/stage3.yaml
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From config/training/stage3.yaml:
```yaml
model_id: facebook/mbart-large-50-many-to-many-mmt
train_csv: data/stage3_train.csv
val_csv: data/stage3_val.csv
epochs: 10
learning_rate: 1e-5
batch_size: 4
gradient_accumulation_steps: 8
warmup_steps: 200
max_source_length: 256
max_target_length: 256
seed: 42
output_dir: models/stage3_final
```

From scripts/training_utils.py (relevant exports):
```python
def load_config(config_path: Union[str, Path]) -> Dict[str, Any]:
    """Load YAML config file."""

def merge_config(config: Dict, cli_args: Dict) -> Dict:
    """Merge CLI args into config dict."""

def validate_config(config: Dict) -> None:
    """Validate config values and paths."""

def write_run_manifest(output_dir: Path, config: Dict, stage: str = "stage1") -> None:
    """Write run manifest JSON."""

def set_seeds(seed: int) -> None:
    """Set all random seeds."""

def create_dataloader(
    csv_path: str, 
    tokenizer_path: str, 
    batch_size: int = 16,
    max_length: int = 512,
    shuffle: bool = True,
    num_workers: int = 0
) -> DataLoader:
    """Create PyTorch DataLoader for stage CSV."""
```

From Phase 5 patterns:
- Checkpoint format: output_dir/epoch_N/ with model.safetensors, tokenizer.json, training_state.pt
- Metrics logging: JSONL format to train_metrics.jsonl
- Dry-run mode: validate config/paths without torch imports
- CLI args: config defaults + CLI overrides
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Stage 3 training CLI with early stopping core</name>
  <files>scripts/train_stage3.py, tests/test_stage3_training.py</files>
  <behavior>
    - Test 1: CLI loads stage3.yaml config and accepts --checkpoint, --early-stopping-patience flags
    - Test 2: Dry-run mode validates paths/config without torch imports
    - Test 3: EarlyStopping class tracks validation BLEU and triggers stop when no improvement for N epochs
    - Test 4: Training saves per-epoch checkpoints to output_dir/epoch_N/
    - Test 5: Genre tags ([LETTER], [DEBT_NOTE], etc.) are validated in input data
  </behavior>
  <action>
    Create scripts/train_stage3.py implementing Stage 3 training with these features:

    1. **CLI Interface**:
       - --checkpoint (required): Path to Stage 2 checkpoint directory
       - --early-stopping-patience (default=3): Epochs to wait for BLEU improvement
       - --dry-run: Validate only, no training
       - Inherit other args from train_stage2.py (--epochs, --lr, --batch-size, --output-dir)

    2. **Early Stopping Implementation**:
       - Create EarlyStopping class in training_utils.py with:
         - __init__(patience=3, min_delta=0.001)
         - should_stop_early(current_bleu) -> bool
         - is_best_checkpoint(current_bleu) -> bool
       - Track best BLEU across epochs
       - Stop training when no improvement for 'patience' epochs

    3. **Genre Tag Validation**:
       - Check that Stage 3 training data contains genre tags
       - Verify tokenizer recognizes [LETTER], [DEBT_NOTE], [CONTRACT], [ADMIN]
       - Log warning if tags missing or not recognized

    4. **Training Loop**:
       - Load Stage 2 checkpoint via --checkpoint flag
       - Use learning_rate=1e-5 from stage3.yaml
       - Save per-epoch checkpoints (model, tokenizer, training_state.pt)
       - Log metrics to train_metrics.jsonl
       - Write run manifest with stage="stage3"

    5. **Test Coverage**:
       - Create tests/test_stage3_training.py
       - Test CLI arg parsing and validation
       - Test early stopping logic (mock validation BLEU)
       - Test dry-run mode
       - Test genre tag validation

    Reference patterns from train_stage1.py and train_stage2.py, adapting for Stage 3 specifics.
  </action>
  <verify>
    <automated>python -m pytest tests/test_stage3_training.py -xvs</automated>
  </verify>
  <done>All tests pass; train_stage3.py exists with working CLI and early stopping skeleton.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate early stopping into training utilities and add genre validation</name>
  <files>scripts/training_utils.py, scripts/train_stage3.py</files>
  <action>
    1. **Add EarlyStopping to training_utils.py**:
       - Implement EarlyStopping class with patience tracking
       - Add should_stop_early() method
       - Add is_best_checkpoint() method
       - Include reset() method for reuse across runs

    2. **Update train_stage3.py**:
       - Import EarlyStopping from training_utils
       - Integrate early stopping into training loop:
         - After each epoch, compute validation BLEU
         - Check if should_stop_early(validation_bleu)
         - If best checkpoint, save to output_dir/best/
         - Log early stopping decision to console

    3. **Add Genre Validation**:
       - Add validate_genre_tags(dataframe, tokenizer) function in training_utils.py
       - Check that genre tags appear at start of transliterations
       - Verify tokenizer.encode/decode preserves tags
       - Log validation results

    4. **Enhance Checkpointing**:
       - Save early_stopping_state.pt with best BLEU, patience counter, epoch
       - Make training resumable from interrupted early stopping

    5. **Update Documentation**:
       - Add docstrings for EarlyStopping class
       - Update train_stage3.py usage examples
       - Document genre tag requirements

    Ensure backward compatibility: Stage 1/2 training scripts still work unchanged.
  </action>
  <verify>
    <automated>python -c "from scripts.training_utils import EarlyStopping; es = EarlyStopping(patience=3); print('EarlyStopping import successful')"</automated>
  </verify>
  <done>Early stopping integrated; genre validation works; train_stage3.py completes training loop with early stopping decisions.</done>
</task>

<task type="auto">
  <name>Task 3: Add comprehensive tests and final validation</name>
  <files>tests/test_stage3_training.py, scripts/train_stage3.py</files>
  <action>
    1. **Expand Test Coverage**:
       - Test early stopping with mock BLEU sequences (improving, plateauing, declining)
       - Test genre tag validation with sample data
       - Test checkpoint loading from Stage 2
       - Test dry-run with various CLI combinations
       - Test resume from interrupted training

    2. **Add Integration Tests**:
       - Test full training loop with small dummy dataset
       - Test early stopping triggers correctly
       - Test best checkpoint saving
       - Test metrics logging format

    3. **Validate Against Requirements**:
       - Verify TRN3-01: "User can train Stage 3 from Stage 2 checkpoint with genre-conditioned inputs and early stopping"
       - Create test that demonstrates requirement satisfaction
       - Document in test docstrings

    4. **Final CLI Polish**:
       - Add --help text describing Stage 3 specifics
       - Add example commands to docstring
       - Validate all error messages are clear
       - Test with invalid inputs (missing checkpoint, wrong genre tags)

    5. **Run Smoke Test**:
       - Execute: `python scripts/train_stage3.py --dry-run --checkpoint models/stage2_final --output-dir models/stage3_test`
       - Should validate config and paths without errors
       - Should print genre tag validation results

    Commit all changes with conventional commits.
  </action>
  <verify>
    <automated>python scripts/train_stage3.py --dry-run --checkpoint models/stage2_final --output-dir models/stage3_test 2>&1 | grep -q "Dry-run completed successfully"</automated>
  </verify>
  <done>All tests pass; dry-run succeeds; Stage 3 training CLI ready for execution with early stopping and genre validation.</done>
</task>

</tasks>

<verification>
1. Run full test suite: `python -m pytest tests/test_stage3_training.py -xvs`
2. Execute dry-run: `python scripts/train_stage3.py --dry-run --checkpoint models/stage2_final --output-dir models/stage3_test`
3. Check early stopping logic: `python -c "from scripts.training_utils import EarlyStopping; es = EarlyStopping(patience=3); test_bleus = [0.1, 0.15, 0.15, 0.14, 0.13]; [print(f'BLEU {b}: stop={es.should_stop_early(b)}') for b in test_bleus]"`
</verification>

<success_criteria>
1. `scripts/train_stage3.py` exists with working CLI for Stage 3 training
2. Early stopping implemented with configurable patience (default 3 epochs)
3. Genre tags validated in training data
4. Per-epoch checkpoints saved with complete training state
5. Test suite covers all major functionality
6. Requirement TRN3-01 satisfied: User can train Stage 3 with genre-conditioned inputs and early stopping
</success_criteria>

<output>
After completion, create `.planning/phases/06-stage-3-competition-fine-tuning/06-01-SUMMARY.md`
</output>