---
phase: 06-stage-3-competition-fine-tuning
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified: 
  - scripts/evaluate_stage3.py
  - scripts/checkpoint_selection.py
  - tests/test_stage3_evaluation.py
autonomous: true
requirements: [TRN3-02]
user_setup: []

must_haves:
  truths:
    - "User can select best Stage 3 checkpoint based on validation BLEU"
    - "User can export final Stage 3 model with all required artifacts"
    - "Checkpoint selection compares all epoch checkpoints and picks highest BLEU"
  artifacts:
    - path: "scripts/evaluate_stage3.py"
      provides: "Stage 3 evaluation CLI with best checkpoint selection"
      exports: ["main", "find_best_checkpoint", "evaluate_checkpoint"]
    - path: "scripts/checkpoint_selection.py"
      provides: "Checkpoint comparison and selection utilities"
      contains: ["compare_checkpoints", "select_best_checkpoint"]
    - path: "tests/test_stage3_evaluation.py"
      provides: "Test coverage for checkpoint selection"
      min_lines: 30
  key_links:
    - from: "scripts/evaluate_stage3.py"
      to: "scripts/train_stage3.py"
      via: "reads epoch checkpoints from Stage 3 training output"
      pattern: "output_dir.*epoch"
    - from: "scripts/checkpoint_selection.py"
      to: "data/stage3_val.csv"
      via: "evaluates all checkpoints on same validation set"
      pattern: "stage3_val"
    - from: "scripts/evaluate_stage3.py"
      to: "models/stage3_final"
      via: "exports best checkpoint as final model"
      pattern: "export.*final"
---

<objective>
Implement Stage 3 evaluation and checkpoint selection to identify and export best model.

Purpose: Automate selection of highest-performing checkpoint for competition inference.
Output: Stage 3 evaluation CLI that compares epoch checkpoints, selects best based on BLEU, and exports final model.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# Reference Phase 4/5 evaluation patterns
@scripts/evaluate_stage1.py
@scripts/evaluate_stage2.py
@scripts/train_stage3.py
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From scripts/evaluate_stage1.py (pattern reference):
```python
def evaluate_checkpoint(
    checkpoint_path: str,
    val_csv: str,
    metrics: List[str] = ["bleu", "chrf"],
    batch_size: int = 16,
    device: str = "cuda"
) -> Dict[str, float]:
    """Evaluate checkpoint on validation data."""
    # Returns dict with metric_name: value

def compute_bleu(predictions: List[str], references: List[str]) -> float:
    """Compute BLEU score."""

def compute_chrf(predictions: List[str], references: List[str]) -> float:
    """Compute chrF score."""
```

From scripts/evaluate_stage2.py (comparison pattern):
```python
def compare_stages(
    stage1_checkpoint: str,
    stage2_checkpoint: str,
    val_csv: str
) -> Dict[str, Any]:
    """Compare Stage 1 vs Stage 2 performance."""
    # Returns dict with stage1_bleu, stage2_bleu, delta_bleu, etc.

def write_comparison_json(output_path: str, comparison: Dict) -> None:
    """Write comparison results to JSON."""
```

From Phase 6 Plan 01 output (train_stage3.py creates):
- Checkpoint directory structure: output_dir/epoch_1/, output_dir/epoch_2/, ...
- Each epoch checkpoint contains: model.safetensors, tokenizer.json, training_state.pt
- Validation metrics logged to train_metrics.jsonl
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement checkpoint selection utilities and Stage 3 evaluation CLI</name>
  <files>scripts/checkpoint_selection.py, scripts/evaluate_stage3.py, tests/test_stage3_evaluation.py</files>
  <behavior>
    - Test 1: find_best_checkpoint() scans epoch directories and returns path to highest BLEU
    - Test 2: evaluate_checkpoint() computes BLEU/chrF for a single checkpoint
    - Test 3: compare_checkpoints() evaluates multiple checkpoints and ranks them
    - Test 4: export_best_checkpoint() copies best checkpoint to final location with manifest
    - Test 5: CLI accepts --checkpoints-dir and --val-data arguments
  </behavior>
  <action>
    Create scripts for Stage 3 checkpoint selection:

    1. **checkpoint_selection.py utilities**:
       - `find_best_checkpoint(checkpoints_dir, val_csv, metric="bleu") -> str`
         - Scans checkpoints_dir/epoch_*/ directories
         - Evaluates each on val_csv
         - Returns path to checkpoint with highest metric value
       - `compare_checkpoints(checkpoint_paths, val_csv) -> List[Dict]`
         - Evaluates all checkpoints
         - Returns sorted list of dicts with {path, bleu, chrf, epoch}
       - `export_best_checkpoint(best_path, export_dir) -> None`
         - Copies best checkpoint to export_dir/
         - Creates best_checkpoint_manifest.json with metadata
         - Updates symlink if needed

    2. **evaluate_stage3.py CLI**:
       - `--checkpoints-dir` (required): Directory with epoch_* checkpoints from Stage 3 training
       - `--val-data` (default=data/stage3_val.csv): Validation CSV for evaluation
       - `--export-dir` (default=models/stage3_final): Where to export best checkpoint
       - `--dry-run`: Evaluate without exporting
       - `--metric` (default=bleu): Metric for selection (bleu or chrf)
       - Output: Prints ranking and exports best checkpoint

    3. **Integration with Stage 3 training**:
       - Read train_metrics.jsonl if available for cached validation scores
       - Fallback to full evaluation if metrics not cached
       - Support resuming interrupted evaluation

    4. **Test Implementation**:
       - Create tests/test_stage3_evaluation.py
       - Mock checkpoint directories for testing
       - Test ranking logic with mock metrics
       - Test export functionality

    Follow patterns from evaluate_stage1.py and evaluate_stage2.py.
  </action>
  <verify>
    <automated>python -m pytest tests/test_stage3_evaluation.py::test_find_best_checkpoint -xvs</automated>
  </verify>
  <done>Core checkpoint selection utilities implemented and tested; evaluate_stage3.py skeleton created.</done>
</task>

<task type="auto">
  <name>Task 2: Add comprehensive evaluation and export features</name>
  <files>scripts/evaluate_stage3.py, scripts/checkpoint_selection.py</files>
  <action>
    1. **Enhance evaluate_stage3.py**:
       - Add progress bars for checkpoint evaluation
       - Implement parallel evaluation of multiple checkpoints (optional)
       - Add detailed logging of evaluation progress
       - Support --batch-size and --device flags

    2. **Improve Checkpoint Selection**:
       - Add tie-breaking logic (use chrf if bleu equal)
       - Consider training loss trends in selection (optional)
       - Validate checkpoint completeness (model + tokenizer files)
       - Handle corrupted or incomplete checkpoints gracefully

    3. **Export Final Model**:
       - Copy best checkpoint to export_dir/
       - Create models/stage3_final/ directory structure:
         - model.safetensors
         - tokenizer.json  
         - config.json
         - best_checkpoint_manifest.json (metadata)
         - evaluation_results.json (validation metrics)
       - Update any symlinks or references

    4. **Genre-Specific Evaluation**:
       - Optionally compute genre-specific BLEU scores
       - Add --genre-breakdown flag to show performance by genre
       - Log genre performance in evaluation_results.json

    5. **Integration with Training**:
       - Support reading from train_metrics.jsonl for faster evaluation
       - Add --use-cached-metrics flag
       - Validate cached metrics against fresh evaluation if discrepancy

    Ensure backward compatibility with existing evaluation scripts.
  </action>
  <verify>
    <automated>python -c "from scripts.checkpoint_selection import find_best_checkpoint; print('Import successful')"</automated>
  </verify>
  <done>Stage 3 evaluation CLI fully functional with checkpoint ranking, selection, and export.</done>
</task>

<task type="auto">
  <name>Task 3: Final validation and integration tests</name>
  <files>tests/test_stage3_evaluation.py, scripts/evaluate_stage3.py</files>
  <action>
    1. **Comprehensive Test Suite**:
       - Test full evaluation pipeline with mock checkpoints
       - Test tie-breaking and edge cases
       - Test export directory creation and file copying
       - Test genre breakdown computation
       - Test integration with train_stage3.py output

    2. **End-to-End Validation**:
       - Simulate Stage 3 training output with dummy epoch checkpoints
       - Run evaluate_stage3.py on simulated data
       - Verify best checkpoint selection logic
       - Verify export directory structure

    3. **Requirement Validation**:
       - Verify TRN3-02: "User can select and export best Stage 3 checkpoint based on validation BLEU"
       - Create demonstration test showing requirement satisfaction
       - Document in test docstrings and README

    4. **CLI Usability**:
       - Test --help output completeness
       - Test error handling for missing/invalid arguments
       - Add example usage to docstring
       - Validate output formatting is clear

    5. **Performance Optimization**:
       - Profile evaluation time for multiple checkpoints
       - Add optional parallel evaluation
       - Implement caching of tokenizer/model loading where possible

    6. **Final Smoke Test**:
       - Create dummy checkpoints: models/stage3_test/epoch_{1..3}/
       - Run: `python scripts/evaluate_stage3.py --checkpoints-dir models/stage3_test --dry-run`
       - Should evaluate and rank checkpoints without errors

    Commit all changes with conventional commits.
  </action>
  <verify>
    <automated>python scripts/evaluate_stage3.py --help 2>&1 | grep -q "checkpoints-dir"</automated>
  </verify>
  <done>All tests pass; evaluate_stage3.py ready for production use; requirement TRN3-02 satisfied.</done>
</task>

</tasks>

<verification>
1. Run test suite: `python -m pytest tests/test_stage3_evaluation.py -xvs`
2. Test CLI help: `python scripts/evaluate_stage3.py --help`
3. Test dry-run with dummy data: `python scripts/evaluate_stage3.py --checkpoints-dir models/stage3_test --dry-run` (create dummy dir first)
4. Verify checkpoint selection logic: `python -c "from scripts.checkpoint_selection import compare_checkpoints; print('Import successful')"`
</verification>

<success_criteria>
1. `scripts/evaluate_stage3.py` exists with working CLI for checkpoint evaluation
2. Best checkpoint selected based on validation BLEU (configurable metric)
3. Final model exported to specified directory with complete artifacts
4. Genre-specific performance breakdown available (optional)
5. Test suite covers all major functionality
6. Requirement TRN3-02 satisfied: User can select and export best Stage 3 checkpoint based on validation BLEU
</success_criteria>

<output>
After completion, create `.planning/phases/06-stage-3-competition-fine-tuning/06-02-SUMMARY.md`
</output>